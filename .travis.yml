language: rust
rust:
  - nightly
before_script:
  - rustup component add rustfmt-preview
  - cargo install clippy
script:
  - cargo build --verbose
  - cargo fmt -- --check
  - cargo clippy
  - cargo test --verbose

